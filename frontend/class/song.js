export default class Song {
    constructor({
        title,
        artist,
        album
    }) {
        this.title = title;
        this.artist = artist;
        this.album = album;
    }
}
